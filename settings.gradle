plugins {
    // Lets Gradle automatically download a JDK 21 toolchain when the machine's
    // default JDK is a different major version.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'backend'
